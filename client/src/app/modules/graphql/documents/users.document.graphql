query GetUserInfo {
  userInfo {
    id,
    username,
    email,
    devices {
      id,
      hostname,
      machineSpecificStamp,
      createdAt
    },
    ipFilters {
      id,
      ip,
      filterAction,
      comment,
      createdAt
    }
  }
}
